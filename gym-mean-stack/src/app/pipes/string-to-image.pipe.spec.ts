import { StringToImagePipe } from './string-to-image.pipe';

describe('StringToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new StringToImagePipe();
    expect(pipe).toBeTruthy();
  });
});
